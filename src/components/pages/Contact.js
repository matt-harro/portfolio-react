import Section from '../layout/sections/Section';
import SectionContent from './../layout/sections/SectionContent';

const Contact = (props) => {
  return (
    <Section>
      <SectionContent>Contact</SectionContent>
    </Section>
  );
};

export default Contact;
